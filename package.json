{
  "name": "entrega-final",
  "version": "1.0.0",
  "description": "Entrega sobre la seleccion argentina de futbol",
  "main": "index.js",
  "scripts": {
    "build-css": "node-sass --include-path scss scss/estilos.scss css/estilos.css",
    "watch-css": "nodemon -e scss -x \"npm run build-css\"",
    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Leandro Mayol",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.20"
  }
}
